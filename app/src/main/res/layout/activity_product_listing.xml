<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="#3cf78585"
    android:padding="10dp">
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Books You might be interested in"
        android:textSize="16sp"
        android:textColor="@color/colorRedDark"
        android:layout_margin="10dp"/>
    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listingSpinner"
        android:entries="@array/sorting"
        android:background="@drawable/curveback"
        android:foregroundTint="#e2e2e2"
        android:padding="5dp"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="10dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true">
    </Spinner>
    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listingList"
        android:layout_below="@+id/listingSpinner">
    </ListView>
</RelativeLayout>